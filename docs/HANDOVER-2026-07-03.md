# Omnivate Cockpit — Session Handover (2026-07-03)

**Scope of this session:** NEXT steps 1 AND 2 from the 07-02 handover —
the **visual-solidity pass** (SHELL-4, NFR-4/5/7), then **Orders & spend
(INFRA-4) + Blacklist status (HEALTH-3)**. Read
`docs/HANDOVER-2026-07-02.md` FIRST for full project context (identity,
data architecture, views, credentials, env gotchas — all still accurate);
this file only covers what changed on top.

---

## 0. Step 2 — Orders & spend (INFRA-4) + Blacklist (HEALTH-3)

**Migration `cockpit_read_models_005`** (applied live, additive):
- `vw_cockpit_orders` — sp_orders + `spent_usd` (= total_cost_usd ONLY when
  status='completed') + `order_date` (coalesce placed_at, created_at).
  **Spend semantics learned from the data:** total_cost_usd on
  failed/superseded/awaiting orders is PROJECTED cost, never charged (the
  23 failed cylindo orders were the 06-18 flap-rejected attempts — $0).
  `actual_deduction` is unreliable (recorded on 1 of 10 completed orders),
  so spend derives from status. Verified: view total = **$184.80** =
  exactly the known real charges ($18.48 smoke + $166.32 scale run).
- `vw_cockpit_blacklist` — pass-through of sp_domain_blacklist_checks
  (per-domain DNSBL cache from the email-infra daily routine).
  Status vocabulary: **listed | clean | couldnt_check**; severity
  high|medium + listed_on zones when listed. 441 domains, 0 listed as of
  07-03; ~35% couldnt_check (zones refuse/timeout — disclosed in UI).

**New surfaces:**
- **`/orders` page** (nav-linked: sidebar + breadcrumbs + ⌘K palette) —
  range selector (30d/90d/all), KPI row (spend / mailboxes purchased /
  domains purchased / awaiting approval with projected $), Spend-by-Client
  table, full orders table (charged vs "projected" vs "$0 · not charged"
  per status). Query layer: `lib/queries/orders.ts`; display atoms:
  `components/orders/order-bits.tsx` (status badge, platform-mix label —
  NOTE platform_mix jsonb keys are case-inconsistent, "GOOGLE" vs
  "google"; normalizePlatformMix() handles it).
- **Client Mailboxes tab** gains two cards: `BlacklistStatusCard`
  (listed domains = loud red table w/ severity + zones + first-listed;
  clean = compact all-clear; couldnt_check disclosed; "Checked {ts}"
  freshness) and `ClientOrdersCard` (per-client spend + last 5 orders +
  link to /orders).

---

## 1. What shipped (commits `9083e2f` + `0144ff3` on main)

### Freshness labeling on every section (SHELL-4 — now DONE, was "slice 3")

- **`components/shared/data-as-of.tsx`** — the standard label, one vocabulary:
  - `facts` mode: "Data as of Tue 1 Jul" (daily/lifetime facts)
  - `sync` mode: "Synced Thu 2 Jul, 07:43 UTC" (mirror tables: mailboxes)
  - `db` mode: "Live view — reads current database state" (alerts, audit, pipelines)
  - optional `liveAt` → "· live to 15:36 UTC" appended (webhook capture)
  - amber **"Sync overdue"** pill when facts lag the expected business day.
    `expectedFactDate()` shifts 9h back before computing (the sync lands
    ~07:43 UTC — facts aren't "overdue" at 6 AM), and expects Friday on
    Sat/Sun/Mon (weekends have no sends by design).
  - All times rendered UTC + deterministic — safe in server AND client
    components (no hydration drift).
- **`components/shared/section-freshness.tsx`** — async server wrapper:
  fetches `getFreshness()` (request-deduped) and renders DataAsOf. Drop-in.
- **Applied on:** Command Center header (facts + live), KPI box, client
  Overview tab (client's own snapshot date), Campaigns tab ("Campaign stats
  synced …"), Mailboxes tab ("Mailbox mirror synced …"), Placement tab
  ("Latest placement test …", no stale warning), Alerts tab + /alerts
  ("Live alerts"), Pipelines tab ("Live pipeline state"), /audit ("Live
  action log"), /compare, /digest (facts + live). The old
  `FreshnessBadge` is deleted — `DataAsOf` is the only vocabulary now.
- **SyncStatusWidget rewritten → "Data Freshness" panel.** The old widget
  showed 4 legacy Trigger.dev task rows with Play/Sync-All buttons that
  are dead post-migration (whitelist trimmed to run-pipeline; tasks target
  the retired mailbox_* model). Now shows reality: daily sync (sp_sync_runs
  status + overdue >26h), facts-through date (+"Behind" pill), live
  send/reply capture recency, recent runs. `/api/tasks/recent-runs` now
  also returns the `vw_cockpit_freshness` row in the same call. Refresh
  button returns with PORT-1. NOTE: `sp_sync_runs.status` vocabulary is
  **success/failed/running** (not Trigger.dev's "completed") — fixed in
  `0144ff3` after prod verification caught the green badge never firing.

### Digest polish

- Formatted date ("Summary for Thursday 2 July"), freshness line with
  live-to, header stacks on mobile.
- Explicit **all-clear cards** when there are no deliverability issues /
  no active alerts (previously the sections silently vanished — reader
  couldn't tell "checked and clean" from "missing"). Same in the
  copy-to-clipboard plain text ("None — …" lines).
- Per-client table gets an empty state; "Sent Yesterday" label corrected
  to "Sent" (the digest date is already stated — "yesterday" was wrong on
  Mondays).

### Empty states / loading / errors (NFR-5)

- New `loading.tsx` for `/digest` + `/compare` (the only async pages that
  lacked one — analytics/health/domains/accounts are pure redirects,
  DEF-4 product decision).
- Campaign table filter empty state upgraded from bare text to a bordered
  explicit message.
- Audit result: everything else already had skeletons + explicit empty
  states + the (dashboard) error boundary. Verified live: the error
  boundary caught a real crash (below) with a proper screen, and slow
  loads show skeletons.

### REAL BUG FIXED — mailboxes tab crashed in production for cylindo

`mailbox-inventory-table.tsx` grouped by lifecycle but had NO group for
**resting** or **parked**, and its fallback pushed to a non-existent
`result["other"]` → `TypeError` → whole tab = error screen. Cylindo has
~50 resting boxes at any time (1-week-active/1-week-rest rotation), so
the tab was down for exactly the client with the most mailboxes. Added
Resting group (violet), an "Other" **catch-all group** so unknown
lifecycles can never crash the tab again, and resting/parked filter
options. (1 cylindo box currently lands in "Other" — odd lifecycle value,
now visible instead of fatal.)

### Responsive (NFR-4) + dark mode (NFR-7)

- **Client tab bar horizontally scrollable at 375px** — Alerts/Settings
  tabs were clipped and unreachable on mobile.
- Settings tab per-day targets: `grid-cols-7` → `2 / sm:4 / md:7`.
- Audit + digest page headers stack on mobile.
- **`lib/design-tokens.ts` now dark-aware** — every ColorTokens value
  carries its `dark:` variant (healthColor / alertSeverityColor /
  runwayColor / replyRateColor feed chips app-wide).
- Dark variants added: client-summary-card status chips, runway-capacity
  callouts, order-status-banner, campaign table dots.
- Compare page: client chips + chart legends/tooltips now use
  `clientLabel()` (was raw slugs — "Acceleration_partners").
- **Deleted `components/analytics/` (13 files) + `freshness-badge.tsx`** —
  legacy standalone-analytics components with zero importers (all
  /analytics routes are redirects); they held the worst dark-mode
  defects. NOTE: a second orphan batch (components/health, /accounts,
  /domains extras, alert-table chain, status-chart, heatmap pair,
  client-health-chart) was identified as dead code but Amzat declined
  deleting it — left in place, unreferenced.

### Verified in dev preview (smoke user, dark + light, 375/768/1280)

Command Center, client cylindo (all 7 tabs), digest, alerts, audit,
compare — no horizontal overflow anywhere, sheet nav works, labels render,
mailboxes tab renders all lifecycle groups. Live bonus: watched the 07-03
morning sync run mid-session — widget showed "running", then facts
flipped Wed 1 Jul → Thu 2 Jul and every label updated.

## 2. Env notes added this session

- The preview browser's screenshot tool times out on pages with the
  TodayLiveStrip (infinite `animate-ping` defeats visual-stability wait) —
  use `preview_snapshot` / `preview_inspect` / eval instead.
- `.next/dev/types/validator.ts` can be left CORRUPTED (torn write) when
  the dev server is killed — breaks `tsc` AND `next build` with nonsense
  errors (e.g. "Cannot find name 'dler'"). Fix: `rm -rf .next/dev`.
- Local force-dynamic SSR renders can take 10-30s (the documented node
  DNS flakiness) — pages show skeletons meanwhile; not an app bug.
- Smoke user password was rotated this session (in-session only, not
  written down — reset again via admin API when needed). Smoke user
  still owed deletion after Omar's review.

## 2b. Step 3 — plugin-side items (executed same day)

**(a) FND-3 sp_mailbox_daily writer — LIVE.** pg_cron installed (1.6.4);
migration `sp_mailbox_daily_snapshot_writer` (+ `_rates_join_fix`) created
`sp_snapshot_mailbox_daily(p_date)` — one row per mailbox per day, upsert
on the table's existing unique (smartlead_account_id, snapshot_date).
State from the sp_mailboxes mirror + previous-day activity from
sp_daily_mailbox_facts + blacklist flag + spam/reply rates from
vw_cockpit_mailbox_rates (NOTE: that view's account_id = sp_mailboxes.id,
NOT the Smartlead account id). Scheduled: cron job
`sp-mailbox-daily-snapshot` daily 09:05 UTC (after the ~07:43 sync and
~08:24 blacklist check). First snapshot verified: 1,430 rows, 403 with
sends summing 2,435 (= exactly yesterday's total), 376 reply rates.
inbox_placement/bounce stay null (no upstream source;
sp_placement_mailbox_summary is empty). HEALTH-4 trends unlock as history
accrues — UI work then.

**(c) Recipient-side send split — LIVE (upgraded from "optional").**
The old "impossible upstream" verdict predated the send-events capture:
sp_send_events.raw_payload->>'to_email' exists on 61k events (June 3 →
today, NOT purged), sp_mx_cache holds 168k classified domains, coverage
99.5%, and events/day == facts sent EXACTLY. Migration
`sp_recipient_send_split_filler` created
`sp_fill_recipient_send_split(p_days)` — UPDATE-only filler for
sent_google/microsoft/other on sp_daily_mailbox_facts (sync keeps owning
row creation; unknown domains count as other). Backfilled 4,443 fact rows
(whole window); every day reconciles with unattributed = 0 (07-02:
859 G / 997 MS / 579 other = 2,435). Cron `sp-recipient-send-split`
daily 09:10 UTC over a rolling 7-day window (self-heals late rows and
zeroed columns after historical resyncs). Cockpit UI follow-up: the
ProviderSplitCard can now grow a sends-by-recipient view.

**(b) PORT-1 refresh — CODE COMPLETE, activation checklist below.**
- `/api/analytics/refresh` now dispatches a GitHub Actions workflow
  (env-gated: without GITHUB_SYNC_TOKEN it stays a clear 501).
- Data Freshness widget has the Refresh button; progress = the new
  sp_sync_runs row appearing in the widget's 60s polling.
- The workflow (checks out Omnivate-AI/smartlead-perf-plugin, runs
  `npm run sync`; manual runs post Slack to the TEST channel) is IN PLACE
  at `.github/workflows/perf-sync.yml` (pushed once access was restored —
  see § 2c).

**PORT-1 activation checklist (Amzat, ~10 min — secrets only):**
1. Repo secrets (cockpit repo → Settings → Secrets → Actions):
   `PERF_PLUGIN_REPO_TOKEN` (fine-grained PAT, read-only Contents on
   Omnivate-AI/smartlead-perf-plugin), `SMARTLEAD_API_KEY`,
   `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SLACK_BOT_TOKEN`,
   `SLACK_CHANNEL_ID_TEST` (C0B4JS55BL1), `SLACK_CHANNEL_ID_PROD`.
   (Values = the plugin's local .env.local.)
2. Vercel env: `GITHUB_SYNC_TOKEN` (fine-grained PAT, Actions read+write
   on the cockpit repo) → redeploy.
3. Test: Actions tab → perf-sync → Run workflow (dry_run=true) → then the
   cockpit Refresh button.
Optional later: uncomment the schedule block for a 12:30 UTC weekday
cloud run (posts the PROD Slack summary — Omar should approve first).

## 2c. ⚠ Access change discovered 2026-07-03 late afternoon

Between the orders deploy (~15:00 UTC) and the PORT-1 push (~17:00 UTC),
**Amzat19's push permission on Omnivate-AI/omnivate-cockpit was revoked**
(API says pull:true push:false) **and the repo flipped private → public**.
Also confirmed: no access from this environment to
Omnivate-AI/omnivate-ai-outbound, Omnivate-AI/smartlead-perf-plugin, or
the Amzat-omni fork ("Repository not found" on all three — likely org
OAuth-app policy plus permission changes). The PORT-1 commit sat LOCAL
on main awaiting `git push`. The DB-side deliverables (FND-3 writer,
send-split filler) were live regardless — they don't touch GitHub.

**RESOLVED same evening:** Amzat restored access; all pending commits
pushed, and the workflow file moved to its real home
(`.github/workflows/perf-sync.yml` — workflow pushes work again too).
Whether the repo should stay PUBLIC is still an open question for Omar
(internal ops docs in docs/).

## 2d. Step 4 — E2E rewrite (DEF-8)

The legacy Playwright suites seeded fake clients into RETIRED legacy
tables via service-role writes and drove removed infra actions
(drain/retire) — quarantined untouched in `e2e/_legacy/` (testIgnore'd).

**New suites are READ-ONLY**: a setup project logs in once as the smoke
user (`E2E_EMAIL`/`E2E_PASSWORD` in .env.local) and shares storage state;
specs assert structure, labeling and API contracts — never exact metric
values, never seeding. **Safe against production**:
`BASE_URL=https://omnivate-cockpit.vercel.app npx playwright test`
(default target = a locally running dev server; `npm run test:e2e`).

Coverage: auth gate + bad-credential error (AUTH-1), sidebar nav
completeness, Command Center (freshness label, live strip, KPIs, Data
Freshness panel + Refresh button, range filter), client detail all-tabs
incl. **the resting-crash regression test on the mailboxes tab**, orders
page (INFRA-4 markers + charged-vs-projected), digest (explicit
all-clear/issue sections), alerts, audit, compare (charts render),
DEF-4 redirect assertions, API contracts (recent-runs shape incl.
freshness; refresh GET/POST honesty incl. the 501-unconfigured path).

## 3. NEXT (order from 07-02; steps 1 + 2 + 3 now done)

1. ~~Visual-solidity pass~~ **DONE (this session)**
2. ~~Orders & spend (INFRA-4) + Blacklist (HEALTH-3)~~ **DONE (this
   session, § 0 above)**
3. ~~Plugin-side items~~ **DONE (§ 2b): FND-3 writer LIVE, send-split
   filler LIVE, PORT-1 code complete — needs the activation checklist +
   push access restored (§ 2c).**
4. E2E rewrite (DEF-8).
5. Backlog: PORT-2/3 depth (incl. sends-by-recipient on ProviderSplitCard
   now that data exists), HEALTH-4 trend UI once sp_mailbox_daily accrues,
   ALERT-3 detail flow, Build-5 actions, AUTH hardening.

**Also still owed:** Loom for ClickUp `869dwvyf0`; delete smoke user;
key rotation at V1 wrap.
