# Omnivate Cockpit — Session Handover (2026-07-06)

**Scope:** the FULL action list from Omar's 2026-07-06 cockpit review
(Fireflies transcript, Omar ↔ Amzat). This was the "with-Omar session"
the 07-03 handover parked Build-5/alert design on. Read
`docs/HANDOVER-2026-07-03.md` for prior context; this file covers what
changed on top. Three commits on main: `a76afda`, `53361a4`, + this one.

---

## What Omar asked for → what shipped

| Omar's ask | Delivered |
|---|---|
| Send Targets component "not useful — take it out" | Removed (chart + component deleted; min-sends alert idea parked) |
| Runway number wrong (PayCaptain 31.8d) + must mirror the perf plugin | Verified faithful to plugin math (the 07-02 Portfolio top-up explains the jump). Real fix = model: `vw_cockpit_client_runway` (007) scopes runway to ACTIVE PRIMARY campaigns (follow-up/referral excluded); gauge shows formula inputs (`N emails ÷ cap/day`) |
| Per-client visual runway slider on Command Center | `LeadRunwayBar` on every client card: completed / in-progress / not-started across active primaries |
| Strip the untrusted alert system, rebuild section by section | Tier taxonomy (008): every alert = actionable vs maintenance. ALL top-line counts (KPI, sidebar badge, cards, banner) count actionable only. Live effect: cylindo 28→6, omnivate 22→0. /alerts defaults to actionable, tier filter added |
| Alert #1: running out of leads, primary campaigns only, finished campaigns don't nag | `cockpit_alerts` + `fn_cockpit_eval_lead_runway` (008): daily 09:20 UTC, one open alert per client, auto-resolves on recovery, `considered_done` override kills false nags. First run opened **cylindo HIGH (0.83 days)** |
| Campaigns: active/past toggle, all campaigns comparable | Tab fetches ALL campaigns (009); Active / Past sections split by real Smartlead status; class chips (primary/follow-up/referral); mark-done toggle per campaign |
| Ready Bank (replaces "not too useful" lead pipeline) | `cockpit_ready_bank_daily` (010, pg_cron 09:12): qualified TAM / verified email / LinkedIn-only / in-campaign / **available**. Card on client overview; real counts feed the Command Center Ready Bank gauge |
| Mailboxes: Group A capacity alone, all combined, reserve + its capacity | `vw_cockpit_rotation_capacity` (011) + card: Group A / Group B / whole pool / reserve bench with emails-per-day + "sending this week" badge |
| Burnt boxes "says zero / not working" | Root cause: card counted lifecycle='burnt' boxes (0 exactly when boxes burn in service). Now leads with `at_risk_in_play` (<97% on in-play lifecycle) + explicit swap/teardown lines |
| Investigate the 2 at-risk items (cylindo + AP) | See § Ops findings below |
| Numbers must explain themselves | Labels/hints added throughout (runway inputs, Ready Bank hints, rotation badges, needs-action lines, past-status chips) |

## Migrations applied live (source in `db/migrations/`)

- **007** `cockpit_campaign_overrides` (class override + considered_done),
  `vw_cockpit_campaign_class`, `vw_cockpit_client_runway`
- **008** `fn_cockpit_alert_tier`, `cockpit_alerts` (+1e9 id band in the
  unified `vw_cockpit_alerts`), `fn_cockpit_eval_lead_runway` + cron
  `cockpit-lead-runway-alert` 09:20 UTC, portfolio-health actionable-only
- **009** `vw_cockpit_campaigns` + campaign_class/considered_done
- **010** `cockpit_ready_bank_daily` + `fn_cockpit_snapshot_ready_bank` +
  cron `cockpit-ready-bank-daily` 09:12 UTC
- **011** `vw_cockpit_rotation_capacity`

Applied via the **Supabase Management API** (`POST
/v1/projects/{ref}/database/query`, token from the email-infra project) —
the session MCP token was dead. NOTE: these are NOT recorded in
supabase_migrations; `db/migrations/*.sql` in this repo is the source of
record. PS 5.1 gotchas hit: UTF-8 body bytes required (em-dash mojibake),
`Get-Content -Raw` string needs re-interpolation before ConvertTo-Json,
git commit messages with embedded quotes must go via `-F file`.

## Cron inventory (all pg_cron, UTC)

| Job | Time | What |
|---|---|---|
| sp-mailbox-daily-snapshot | 09:05 | FND-3 mailbox daily (07-03) |
| sp-recipient-send-split | 09:10 | recipient split filler (07-03) |
| **cockpit-ready-bank-daily** | 09:12 | NEW — ready-bank counts |
| **cockpit-lead-runway-alert** | 09:20 | NEW — lead-runway eval |
| **cockpit-send-floor-alert** | 09:25 | NEW (07-07, migration 012) — R7 revision: weekday sends below `sp_clients.min_daily_send_volume` → send_floor alert; weekends skipped entirely (Friday alerts survive to Tuesday's eval). First run fired paycaptain (1,555 < 3,000). Cylindo/omnivate floors unset = unmonitored. |

## Ops findings (Omar flagged in the meeting — hand to email-infra ops)

1. **Cylindo REAL BURN, still sending:** `jostein@digitalcylindopro.com`
   (81%) + `jostein.pedersen@digitalcylindopro.com` (82%) — ACTIVE,
   Group A, GOOGLE, same domain. Action: park both + swap 2 of the 49
   reserves via the email-infra rotation; domain → burnt.
2. **AP is NOT a live burn:** `jonathan@emailaccelerationpartners.com`
   is a stale mirror row (created + last_synced 06-16, warmup 0%).
   Verify it still exists in Smartlead; likely plugin-side sync gap.
3. **"Why weren't we flagged": we were.** burn_detected ×2 (06-24) +
   warmup_low_rep ×2 (06-30) sat OPEN in sp_infra_alerts, drowned among
   28 open cylindo alerts (21 reconnect/reapply noise). The tier rebuild
   is the systemic fix; those 4 now surface in a 6-alert actionable view.
4. **Cylindo runway 0.83 days** BUT the Ready Bank shows **5,597
   verified untouched leads** — the refill exists, load it.
5. 11 cylindo burnt domains awaiting teardown decision (InboxKit money).

## Still open / next

- **Build-5 in-app actions** (Omar's #1 strategic priority): design =
  dispatch the email-infra plugin engines (order-engine / adopt-order /
  rotation — already Slack-approval-gated) via GitHub Actions
  workflow_dispatch, same pattern as PORT-1. Cockpit stays a
  trigger+status layer; no second source of truth for infra mutations.
  Not started — needs the workflow + repo secrets + guarded UI.
- **PORT-1 activation** still owed (Amzat, ~10 min secrets — 07-03 § 2b).
- REQUIREMENTS.md needs a delta pass (CC-5 send chart removed, CD-1
  funnel→Ready Bank, section E alert model superseded).
- New-client onboarding for the enumerated blocks in
  fn_cockpit_snapshot_ready_bank + vw_cockpit_client_runway thresholds.
- Smoke user deletion + key rotation at V1 wrap (carried from 07-03).
- e2e: 34/35 green; analytics-redirect spec flaky on cold SSR (known).
