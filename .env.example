# Supabase project (uivgowblojtyiobhgjlv) — the shared sp_* data layer
NEXT_PUBLIC_SUPABASE_URL=https://uivgowblojtyiobhgjlv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=            # anon key (browser: login/realtime only)
SUPABASE_SERVICE_ROLE_KEY=                # server-only: all data reads

# Optional — only needed for the pipeline run button (Trigger.dev run-pipeline)
TRIGGER_SECRET_KEY=
